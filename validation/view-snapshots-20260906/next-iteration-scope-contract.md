# 下一轮：对称性作用域、局部投影与实际轨道详情

状态：在 NUM-FIX-004 冻结取证期间进行的只读源码分析和实施准备。当前构建为 `70e72ef9d4c956b374a36efb4a478489127bca90`；此文件不修改生产算法、阈值、参考输入或本轮裁决。REF-001 继续暂停。

## 已核对的源码链

- `ligand_field.cpp` 的 `initial_orbital_groups` 从普通 metadata.symmetry 建组，`recover_local_group_symmetry` 又通过局部中心 s/p/d 分类或维数先验恢复 group.symmetry。`apply_local_ligand_field_symmetry` 在 locally_classified 时覆盖普通 metadata.symmetry。头文件声称不覆盖 producer 标签，与该分支行为不一致。
- `propagate_spin_counterpart_labels` 通过互为最佳的 α/β 重叠配对补空字符串，未保存“从另一个自旋推断”的身份或置信信息。
- `mo_diagram_chemistry.cpp` 中还存在单独的局部恢复、合并自旋标签、Oh/Td 金属成分加维数的回退，以及恢复结果写回逐 MO metadata 的循环。仅修改一处赋值不足以解决 SYM-004/005。
- `classify_local_metal_irrep` 汇总逐壳、逐 MO 的系数平方，返回标签分数占比；当前最低 copy confidence 为 0.55。它没有利用 AO 重叠矩阵的径向交叉项，不能直接称作完整波函数的 L2 投影或严格全局 irrep。
- `draw_level_tooltip` 已按被选成员显示能量和自旋，但后续组成、局部几何中心选择等仍读组级 chemistry。当前提示框还同时列出六种能量单位以及多段详情，造成已记录的裁切。原始成员详情与组解释仍需分开。
- `main.cpp` 的 3D 渲染使用整窗 viewport，侧栏随后覆盖其左侧；当前能级 canvas 高度与外层 card 高度相同，控制区及摘要又占据额外高度。因此这些问题有共同布局原因。

## 共享对称性记录

1. 保留逐 MO 的原始身份、能量、占据、自旋、producer 标签及已通过全局操作检查的派生标签。兼容 symmetry 字段不再承载局部替代解释。
2. 每条解释有显式作用域：全分子 MO、全分子 MO 子空间、指定中心的 AO 投影、近似局部母群模型或自旋伙伴推断。局部解释保存中心、完整第一壳、几何模型、参考坐标到输入坐标的旋转、原始成员及来源方法。
3. 维数兼容只提供候选；不与真实 AO 投影使用同一种来源或“严格”状态。整体与局部点群同名，也不代表坐标轴、原子范围、操作或电子子空间相同。
4. α/β 伙伴链接保存源成员、目标成员、重叠和竞争匹配的间距。目标 MO 有自身证据时优先使用自身证据；伙伴推断不伪装为目标的 Gaussian 原始标签。
5. 能级组保留选用哪一条解释的链接。比较 irrep、合并组或寻找 π 伙伴前，先比较点群、坐标轴与原子/子空间作用域。普通浏览器与局部配位场图分别明确其显示含义。

## 局部投影的数值方案与待检构造

从已独立积分验证的 AO 度量 S 出发。若 X 的列描述指定 AO/局部 irrep 子空间，可在度量中建立投影：

`P_X = X (Xᵀ S X)^+ Xᵀ S`。

对一个已归一化的 MO 子空间 C，投影量来自 `tr(Cᵀ S P_X C)`。记录局部中心覆盖率、所选 irrep 在局部中心内的比例、剩余混合部分、矩阵秩和残差。投影到不同中心的非正交空间通常不能直接相加为总布居；不得把这个量重新命名为一种守恒原子分区。伪逆的数值秩必须有诊断，不能借此隐藏原 AO 空间或丢掉难以计算的方向。

待加入程序检查的解析构造：两条归一化的同角向径向函数，径向重叠为 0.99；x 通道系数为 `(0.5,0)`，y 通道为 `(-1,1)`。系数平方的 x/y 和为 `0.25/2`，但保留径向交叉项后的物理范数为 `0.25/0.02`。两种“主导通道”会相反。整个函数可按总范数 0.27 归一化。该构造说明待检的数学差异，尚未当作本库新失败记录，也不是新的外部分子。

控制还须覆盖完整/不完整简并空间、近简并混合、刚体旋转、原子重排、轨道变号、简并换基、等价径向重表示、纯/笛卡尔 d 壳、重复径向收缩和 β 伙伴。对真实案例，允许经过验证的“局部混合/近似”结果；不强迫唯一标签。

## π 伙伴和晶场间隔

`find_pi_pairs` 中，同对称性 π 伙伴与不同 irrep 的 E/T2 或 Eg/T2g 弱晶场间隔目前共用 RawPiPair/PiInteractionDescriptor，并共同显示 Δπ。下一轮需要独立的晶场能隙记录，保存两组各自的 irrep、能量统计和局部环境。保持有依据的弱扰动近似非键解释；不把晶场间隔改称 π 伙伴分裂。

SigmaOnly 配体目录作为先验保留，但现有硬 continue 不允许它推翻直接轨道证据。共享记录要分别保存先验及实际证据、冲突状态和适用原子对；没有支持的 P 配体正式输入仍是覆盖缺口，不能标为已验证。

## 图结构边界

既有 topology-v1 契约继续有效。已重新取得 IUPAC 的原始定义：螺连接要求两环只共用一个原子，自由螺连接还要求没有其他直接或间接连接。多方向 π 通道本身不是螺环依据。[IUPAC spiro union](https://goldbook.iupac.org/terms/view/S05884)

环见证、π 方向和电子耦合分别保存；同一双连通块中仍可能存在带附加桥联系的螺连接。不能用“没有割点”直接排除全部螺连接。OLD-107 正例继续保留；TOPO-002 不恢复已经撤回的 OLD-013 第一壳/CN 算错结论。

## 实施与验收顺序

本轮全部收集和复核终止后，先统一分析 NUM-FIX-004，再实施上述共享接口及消费者迁移。短提示框显示实际选中 MO；详细信息在可滚动的选中详情区完整展开，分别列出组解释与实际 MO 的数据和能量范围。3D 视区和能级 canvas 的实际可用范围需由布局计算，并记录真实裁切矩形。

新的 PNG/SVG/JSON/CSV 快照接口作为共同出口继续使用。自报标签一致性、绘制调用一致性和部分像素探针均不能替代完整文字/图像验收。生产改动后仍重新冻结并运行全部 273；外部正式集合加入后必须运行全部正式案例。
