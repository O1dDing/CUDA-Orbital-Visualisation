# 当前视图快照修复与 NUM-FIX-004

构建身份：`70e72ef9d4c956b374a36efb4a478489127bca90`。包含 `94447b0` 的对称性修复和 `5bbc23e` 的当前视图快照修复。

真实导出按钮直接导出已经绘制的不可变快照。图表行选择与当前检查的 MO 独立保存；完整成员、β 对应轨道、选中标记、每成员电子箭头和实际能量范围在屏幕及输出之间可追溯。JSON/CSV 数值输出保留 double 往返精度。原有 `COV_VALIDATION 1` 操作继续兼容；新增 `export-name` 仅命名证据，导出仍需实际按钮。

普通版/验证版各 34 项组件检查全部通过，含真实 ImGui 能量轴控件路径。OLD-001 的完整试运行取证 11.7034204 秒，独立数值复核 1.9584581 秒。7 组导出状态检查全部通过；它们关联到实际导出帧和同一快照，并检查 PNG 选中线条的实际像素。

原 273 的 NUM-FIX-004 已冻结并启动。`status.json` 是发布时间的快照；权威活动进度在其指定的本地目录。完整收集的所有案例终止且进程树退出后，工作流顺序完成数值复核和多状态导出复核。所有失败保留，轮内不修改输入、程序或阈值。

这不是最终验收发布。正式完整通过数仍为 0，外部分子 0，REF-001 按用户要求保持暂停。NUM-FIX-003 的 273 数值通过结果属于 a9bf30a，不能算成本版的全量结果。全局/局部标签边界、图拓扑、π 与晶场区分、完整文字/裁切/图像检查及原始物理参考仍待处理。`next-iteration-scope-contract.md` 是只读实施准备，没有修改正在取证的版本。

此目录上传紧凑的源码、流程、身份和试运行报告，不包含 NUM-FIX-004 的全部纹理、截图及参考网格。此前已经上传并验证的 REF 暂停交接 release 保持完整。
