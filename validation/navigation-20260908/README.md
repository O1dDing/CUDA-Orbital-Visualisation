# 真实成员导航通解与下一轮验证准备

NUM-FIX-005 已完成全部 273 案例采集和统一复核：273 数值子集通过；249 界面子集通过、24 失败。所有 1,943 个导出快照包通过，但 72 个成员点击及对应悬停没有到达目标，保留为 NAV-001。该轮证据保持原样。

本次驱动根据目标与每一层窗口在各轴的真实相对位置决定滚动，仅对需要移动的轴发送输入。滚动位置限制在实际可见区域；鼠标移动与滚轮分帧处理，坐标遵守 ImGui 的整数像素规则。没有直接设置滚动值、轨道选择或控件返回值。

新组件测试使用真实 ImGui 按钮和嵌套窗口，在 1.0 与 1.5 缩放下覆盖横向溢出、父窗口裁切内层横向滚动条、双轴往返，以及逐帧鼠标移动、滚轮和点击。横向缺口不得引起无关的纵向对齐。导航尝试上限继续为 60。

后续顺序为同一提交的 ON/OFF 构建、完整组件检查、试运行、固定版本全 273 案例取证。全部案例终止后统一数值与界面复核，才裁决 NAV-001；当前不提前关闭问题。首个案例及萘分别记录实际阶段耗时，采集时间不冒充全部科学验证耗时。

本任务调度上限 4 个物理核、64 GiB，使用 12 核配额中的后 4 个核心位置；批量为 2 个进程，每个 2 核、24 GiB。为用户独立恢复的 Gaussian 工作流保留 8 核、48 GiB；其阶段边界暂停请求由原工作流处理。本任务不修改暂停文件，也不控制该队列。全部计算合计最多 12 核、112 GiB。

此提交是待全库验证的修复实现，不是最终验收版本。物理参考、其他科学与交互待办以及至少 50 个新外部分子的正式验证仍未完成，正式通过数继续为 0。
